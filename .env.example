# Outlook / Office 365 SMTP — only credentials needed (Mixpanel is handled via MCP)
# SMTP_USER     = your Outlook email address
# SMTP_PASSWORD = your Outlook password or app password
#   (If MFA is enabled, generate an app password in your Microsoft account settings)
SMTP_USER=aditya.singh@hungama.com
SMTP_PASSWORD=your-outlook-password-or-app-password
